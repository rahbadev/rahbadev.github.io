# ✅ Testing Checklist - دليل الاختبار

## قائمة التحقق السريعة

استخدم هذا الدليل للتأكد من أن جميع التحسينات تعمل بشكل صحيح.

---

## 🧪 الاختبارات الأساسية

### 1. تحميل الصفحة الرئيسية ✓

**الخطوات:**
1. افتح `site/index.html` في المتصفح
2. افتح Developer Tools (F12)
3. اذهب إلى تبويب **Console**

**النتيجة المتوقعة:**
```
✅ تم تحميل الشركة بنجاح
⏱️ تحميل الخدمات: XXXms
✅ تم تحميل الخدمات بنجاح
```

**إذا رأيت أخطاء:**
- تأكد من أن ملفات `logger.js` و `data-service.js` محملة
- تحقق من مسار ملف `services.json`

---

### 2. عرض الخدمات ✓

**الخطوات:**
1. في نفس الصفحة، انتقل إلى قسم "خدماتنا"
2. تحقق من ظهور جميع الفئات (5 فئات)

**النتيجة المتوقعة:**
- ✅ التصميم الجرافيكي (3 خدمات)
- ✅ المواقع الإلكترونية (3 خدمات)
- ✅ التطبيقات (3 خدمات)
- ✅ جداول بيانات (2 خدمات)
- ✅ خدمات إضافية (6 خدمات)

**إذا لم تظهر الخدمات:**
- افتح Console وابحث عن أخطاء
- تحقق من ملف `services.json` بوجود البيانات

---

### 3. الحاسبة (Calculator) ✓

**الخطوات:**
1. افتح صفحة الحاسبة (إذا كانت موجودة منفصلة)
2. افتح Console
3. اختر بعض الخدمات
4. اضغط "احسب"

**النتيجة المتوقعة في Console:**
```
⏱️ تحميل بيانات الحاسبة: XXXms
✅ تم تحميل بيانات الحاسبة
```

**في الواجهة:**
- ✅ ظهور جميع الخدمات
- ✅ حساب التكلفة بشكل صحيح
- ✅ رسوم العجلة تضاف عند التفعيل

---

## 🔍 الاختبارات المتقدمة

### 4. التخزين المؤقت (Cache) ✓

**الخطوات:**
1. افتح `site/index.html`
2. في Console، اكتب:
   ```javascript
   dataService.clearCache()
   ```
3. أعد تحميل الصفحة
4. لاحظ الوقت في "⏱️ تحميل الخدمات"
5. أعد تحميل الصفحة مرة أخرى
6. لاحظ الوقت مرة أخرى

**النتيجة المتوقعة:**
- التحميل الأول: بطيء نسبياً (fetch من الخادم)
- التحميل الثاني: سريع جداً (من Cache)

---

### 5. معالجة الأخطاء ✓

**الخطوات:**
1. في Console، اكتب:
   ```javascript
   dataService.getServiceById('خدمة-غير-موجودة')
   ```

**النتيجة المتوقعة:**
```
❌ الخدمة غير موجودة: خدمة-غير-موجودة
```

---

### 6. حساب التكلفة ✓

**الخطوات:**
1. في Console، اكتب:
   ```javascript
   dataService.calculateCost(['logo', 'website'], false)
   ```

**النتيجة المتوقعة:**
```javascript
{
  base: 200,      // مجموع أسعار الخدمات
  urgent: 0,      // لا توجد رسوم عجلة
  total: 200
}
```

2. جرب مع العجلة:
   ```javascript
   dataService.calculateCost(['logo', 'website'], true)
   ```

**النتيجة المتوقعة:**
```javascript
{
  base: 200,
  urgent: 100,    // 50% من القاعدة
  total: 300
}
```

---

## 📱 اختبار المتصفحات المختلفة

### الحد الأدنى المدعوم:
- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+

**الخطوات:**
1. افتح الموقع في كل متصفح
2. تحقق من عمل جميع الوظائف
3. تحقق من Console للتأكد من عدم وجود أخطاء

---

## 🚀 اختبار الأداء

### قياس وقت التحميل

**الخطوات:**
1. افتح `site/index.html`
2. افتح Developer Tools → **Network** tab
3. أعد تحميل الصفحة (Ctrl+R)
4. لاحظ:
   - حجم `services.json`
   - وقت تحميل `data-service.js`
   - وقت تحميل `logger.js`

**معايير الأداء الجيد:**
- services.json: < 10KB
- data-service.js: < 5KB
- logger.js: < 5KB
- إجمالي وقت التحميل: < 500ms

---

## 🎨 اختبار التصميم (Responsive)

### الأجهزة المختلفة

**الخطوات:**
1. افتح Developer Tools (F12)
2. اضغط على أيقونة الجوال (Ctrl+Shift+M)
3. جرب الأحجام التالية:
   - 📱 Mobile: 375x667
   - 📱 Tablet: 768x1024
   - 💻 Desktop: 1920x1080

**النتيجة المتوقعة:**
- ✅ جميع الخدمات مرئية
- ✅ لا يوجد تداخل في النصوص
- ✅ الأزرار قابلة للنقر
- ✅ الصور بحجم مناسب

---

## 🔧 استكشاف الأخطاء الشائعة

### ❌ "dataService is not defined"

**الحل:**
```html
<!-- تأكد من الترتيب الصحيح -->
<script src="../shared/js/logger.js" defer></script>
<script src="../shared/js/data-service.js" defer></script>
<script src="assets/js/app.js" defer></script>
```

---

### ❌ "Failed to fetch services.json"

**الأسباب المحتملة:**
1. الملف غير موجود في `site/data/services.json`
2. المسار النسبي خاطئ
3. مشكلة في صلاحيات الملف

**الحل:**
```javascript
// تحقق من المسار في data-service.js
const url = this._resolvePath('data/services.json');
console.log('Loading from:', url);
```

---

### ❌ الخدمات لا تظهر

**الأسباب المحتملة:**
1. عنصر `#servicesContainer` غير موجود
2. خطأ في هيكل JSON
3. مشكلة في CSS

**الحل:**
```javascript
// في Console:
const data = await dataService.getServices();
console.log(data); // تحقق من البيانات

const container = document.getElementById('servicesContainer');
console.log(container); // تحقق من العنصر
```

---

## 📊 قائمة التحقق النهائية

قبل النشر، تأكد من:

### ملفات البيانات
- [x] `site/data/services.json` موجود وصحيح
- [x] `shared/data/company.json` موجود
- [x] جميع الملفات `.old` محذوفة

### JavaScript
- [x] `shared/js/data-service.js` محمّل
- [x] `shared/js/logger.js` محمّل
- [x] `site/assets/js/app.js` محدّث
- [x] `site/assets/js/calculator-new.js` محدّث
- [x] لا توجد `console.log` في الكود

### HTML
- [x] جميع الصفحات تحمّل المكتبات الجديدة
- [x] ترتيب تحميل الـ scripts صحيح

### الوظائف
- [x] تحميل معلومات الشركة
- [x] عرض الخدمات
- [x] حساب التكلفة
- [x] التخزين المؤقت (Cache)
- [x] معالجة الأخطاء

### الأداء
- [x] أوقات التحميل معقولة
- [x] لا توجد تسريبات للذاكرة
- [x] Cache يعمل بشكل صحيح

### التوثيق
- [x] REFACTORING-CHANGELOG.md موجود
- [x] QUICK-START.md موجود
- [x] هذا الملف (TESTING-CHECKLIST.md) موجود

---

## 🎯 الخطوات التالية

بعد اجتياز جميع الاختبارات:

1. ✅ حذف الملفات `.deprecated` نهائياً
2. 📝 تحديث باقي الصفحات (bio, projects, apps)
3. 🎨 البدء في المرحلة 2: تقسيم CSS
4. 🚀 تحسينات الأداء
5. 📱 تحسين الـ SEO

---

**آخر تحديث:** 2026-02-05  
**بواسطة:** GitHub Copilot  
**حالة الاختبار:** ✅ جاهز للاختبار
